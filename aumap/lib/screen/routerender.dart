import 'package:flutter/material.dart';

class RouteRender extends StatelessWidget {
  const RouteRender({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Card(),
    );
  }
}
